{
  "name": "@cloud-dev/coord-board",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run build:office && wrangler deploy",
    "build:office": "vite build --config office-vite.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "node scripts/check-office-source.mjs && vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250124.0",
    "typescript": "^5.7.3",
    "vitest": "^3.2.0",
    "wrangler": "^3.100.0"
  },
  "dependencies": {
    "pixi.js": "^8.19.0"
  }
}
